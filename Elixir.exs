# Elixir

IO.puts("Hello, world!")

"""
Narrative:
1. The program accesses the IO module, which provides input/output functions.
2. It calls the puts function from the IO module.
3. The argument "Hello, world!" is given to puts, defining what will be printed.
4. When run, the function will output the string to the console followed by a newline.
"""
